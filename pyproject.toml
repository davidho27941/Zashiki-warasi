[project]
name = "zashiki-warasi"
version = "0.1.0"
description = "AI agent toolkit with a Gmail polling email agent."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.4",
    "click>=8.1",
    "fastapi>=0.115,<0.120",
    "google-api-python-client>=2.197.0",
    "google-auth-httplib2>=0.4.0",
    "google-auth-oauthlib>=1.4.0",
    "html2text>=2025.4.15",
    "httpx>=0.28.1",
    "langchain-openai>=1.3.2",
    "langgraph>=1.2.6",
    "langgraph-checkpoint-postgres>=3.1.0",
    "notion-client>=3.1.0",
    "opentelemetry-api>=1.30,<2.0",
    "opentelemetry-exporter-otlp-proto-grpc>=1.30,<2.0",
    "opentelemetry-instrumentation-fastapi>=0.51b0,<1.0",
    "opentelemetry-instrumentation-httpx>=0.51b0,<1.0",
    "opentelemetry-instrumentation-psycopg>=0.51b0,<1.0",
    "opentelemetry-sdk>=1.30,<2.0",
    "pdfplumber>=0.11.10",
    "prometheus-client>=0.20,<1.0",
    "psycopg[binary]>=3.2,<3.3",
    "pydantic-settings>=2.14.2",
    "sqlalchemy>=2.0.51",
    "uvicorn[standard]>=0.30,<0.35",
]

[project.scripts]
zashiki-warasi = "zashiki_warasi.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zashiki_warasi"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra --strict-markers"
