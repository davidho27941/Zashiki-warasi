apiVersion: v1
kind: Service
metadata:
  name: {{ include "zashiki-warasi.fullname" . }}
  labels:
    {{- include "zashiki-warasi.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zashiki-warasi.selectorLabels" . | nindent 4 }}
